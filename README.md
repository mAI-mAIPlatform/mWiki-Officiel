# 🌟 mWiki : Le Cerveau du Hub Digital

Bienvenue dans le repo central de **mWiki**. C'est ici que toute la doc de l'écosystème (mSearch, mAINews, mBattle, mAI) est stockée, versionnée et déployée.

## 🚀 La Tech Stack
* **Interface :** HTML/CSS/JS avec design **Liquid Glass** (Flou, transparence, minimalisme).
* **Contenu :** 100% Markdown. Rapide à écrire, facile à lire.
* **Hébergement :** Déploiement automatisé via Vercel & GitHub Actions.

## 🛠️ Lancer en Local
1. Clone le repo : `git clone https://github.com/ton-user/mWiki.git`
2. Ouvre le dossier : `cd mWiki`
3. Lance VS Code : `code .`
4. Utilise l'extension **Live Server** sur `index.html` pour voir le rendu avec le Liquid Glass.

## 📁 Architecture
- `/docs` : Tous les fichiers `.md` classés par catégorie (IA, Dev, DevOps, Tools).
- `/scripts` : La logique JS (mSearch engine).
- `.github/workflows` : Les fichiers YAML pour l'automatisation.
